# `_archive/` — arkiverte rot-filer (ikke slettet)

Flyttet hit under opprydding for å holde prosjektroten ren.
**Ingenting er slettet.**

- **`root_scratch/`** — engangs hjelpe-/kjøreskript som lå i roten
  (`_make_winddir_hh.py`, `_run_droop_diag_plots.py`,
  `_run_gt_trip_loadshed.py`, `_ss_analysis.py`).
- **`run_artifacts/`** — kjøre-utdata som regenereres automatisk
  (`fort.10`, `fort.30`, `OpenFAST_FMU.res`, `fmu_log.txt`,
  `stdout_*.txt`, `*.log`, `_*_out.txt`).

Beholdt i roten: FMU-ene (`fast.fmu`, `fast_debug.fmu`), `src/`, `LEOGO/`,
`test1002/`, konfigurasjon (`pyproject.toml`, `requirements.txt`, `uv.lock`).
